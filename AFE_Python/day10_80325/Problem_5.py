# Given an array nums of size n return the majority element. The majority element is the one appearing more than n/2 times

nums = list(map(int,input("Enter the numbers: ")))
sorted.nums()
#Use sorting approach
def majority():
    count = 0
